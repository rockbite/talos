import {GameScript} from "../../se/gamescript";

class %TEMPLATE_NAME% extends GameScript {

    create(): void {
    }

    update(delta: number): void {
       
    }
}

export {
    %TEMPLATE_NAME%
}
